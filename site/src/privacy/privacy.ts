// Privacy notice page. Static document, no form logic — the only interactivity is
// the browser's native links. This entry exists solely to pull in the page styles
// (which reuse the shared tokens/base/effects from the landing bundle).
import './privacy.scss';
