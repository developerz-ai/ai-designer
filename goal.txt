Instrument this Chrome extension (ai-designer) with GlitchTip error tracking using the Sentry browser SDK, and add the GlitchTip MCP config.

Requirements:
1. Add @sentry/browser as a dependency.
2. Initialize Sentry as early as possible in each of the extension's entry points (recon the real entry points from the manifest/build config - e.g. background service worker, content scripts, popup). Use this public DSN: https://bf037adaf792452d8b77377abb682bd4@glitchtip.infra.developerz.ai/2. It is a GlitchTip ingest key and is SAFE to commit - it ships in the extension bundle. Guard the init so it no-ops when the DSN is empty/undefined.
3. MV3/CSP: ensure the ingest host glitchtip.infra.developerz.ai is permitted (host_permissions / connect-src as needed) so events can be sent from the extension.
4. Capture unhandled errors and unhandled promise rejections. Set environment (e.g. production) and a release identifier.
5. Add a file .mcp.json at the repo root with exactly: {"type":"http","url":"https://glitchtip.infra.developerz.ai/mcp"}
6. Do NOT break the existing build, lint, tests, or extension loading. Keep the change minimal and idiomatic to the existing codebase.

Acceptance: the extension builds, lint and tests pass, Sentry initializes without breaking extension load, and .mcp.json is committed at the repo root. Context: this fulfills developerz-ai/infrastructure issue #497 (GlitchTip per-app rollout); the GlitchTip project id is 2.
